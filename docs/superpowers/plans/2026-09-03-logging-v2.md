# 日志规范化改造 v2 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans. Steps use checkbox syntax.

**Goal:** requestId 全链路串联 + 统一格式 + 中文动作日志（测试可读）+ SQL 噪音治理 + 可选 Graylog(GELF) + LOGGING.md。

**计划文件:** `docs/superpowers/plans/2026-09-03-logging-v2.md`（完整 4 任务 TDD 步骤已写入）

## 任务概览

- **T1** logging_setup 改造：统一 Formatter（yyyyMMdd HH:mm:ss,SSS）、RequestIdFilter(contextvars)、get_logger、GELF UDP handler（自写零依赖）、config 字段（LOG_LEVEL/SQLALCHEMY_LEVEL/GRAYLOG_HOST/PORT）、main.py 启动治理（sqlalchemy=WARNING、关 uvicorn.access、stdout UTF-8）
- **T2** requestId 中间件：透传 X-Request-Id（无则 uuid4）、响应头回写、规范访问行（HTTP method path -> status, requestId, costMs, client, body 摘要 2KB+敏感打码）、SSE 端点跳过访问行、SSE established/closed 日志带 requestId
- **T3** 中文动作日志：全局异常 handler 带 requestId；主干 service ~30 处（元素库/AI生成三阶段/转脚本/执行/自愈/回归/白盒/知识库/推送）开始/成功/失败+原因=/建议=
- **T4** docs/LOGGING.md + 三场景前后对比 + 四项验收自测

## 已确认决策

改造现有 logging_setup.py（不新建）；SSE 事件 data 不动；graypy→自写 GELF；requestId 透传上游 X-Request-Id；Celery worker 无上下文 requestId="-"（按 session_id 串联）。
