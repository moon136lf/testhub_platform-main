# 元素库增强 P1（过滤+登录态卡片+分组入库）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 一次性抓取增强——文本/类型过滤、调试模式、登录态卡片、结果分组勾选入库、bounding box 坐标入库（P2 高亮点选的前置）。

**Spec:** `docs/superpowers/specs/2026-09-03-element-enhancement-design.md`（P1 部分）

**测试约定:** 同步 def + asyncio_run；mock gateway/db；真实文件 fixture。

---

### Task 1: 后端过滤参数 + 调试模式（fetch 链路）

**Files:**
- Modify: `backend/app/schemas/element_schema.py`（ElementFetchRequest 增 3 字段）
- Modify: `backend/app/tasks/element_tasks.py`（扫描后过滤）
- Test: `backend/tests/test_element_tasks.py`（追加）

- [ ] Step 1: 追加失败测试

```python
class TestFetchFilters:
    def test_text_filter_keeps_matching(self):
        """text_filter 逗号分隔, 命中 element_text 保留."""
        from app.tasks.element_tasks import _apply_filters
        elems = [{"element_text": "新增设备"}, {"element_text": "删除"}, {"element_text": "搜索"}]
        out = _apply_filters(elems, text_filter="+,删除", type_filter="", debug_mode=False)
        assert len(out) == 1 and out[0]["element_text"] == "删除"

    def test_type_filter(self):
        from app.tasks.element_tasks import _apply_filters
        elems = [{"element_type": "button", "element_text": "a"},
                 {"element_type": "input", "element_text": "b"}]
        out = _apply_filters(elems, text_filter="", type_filter="button", debug_mode=False)
        assert len(out) == 1 and out[0]["element_type"] == "button"

    def test_debug_mode_bypasses_filters(self):
        from app.tasks.element_tasks import _apply_filters
        elems = [{"element_type": "span", "element_text": "x", "debug": {"svgHref": "", "iconFingerprint": "abc"}}]
        out = _apply_filters(elems, text_filter="不存在", type_filter="button", debug_mode=True)
        assert len(out) == 1  # debug 模式不过滤
```

- [ ] Step 2: 确认失败（ImportError: _apply_filters）

- [ ] Step 3: 实现

`element_tasks.py` 加 `_apply_filters(elements, text_filter, type_filter, debug_mode)`：
- debug_mode=True 原样返回
- text_filter 非空：按逗号切词，任一词是 element_text 子串则保留（无 element_text 的丢弃）
- type_filter 非空：按逗号切类型白名单，element_type 不在名单则丢弃

`_fetch_elements_async` 在 `scan_interactive_elements` 返回后调用（`raw_elements = _apply_filters(raw_elements, ...)`），参数从 task 入参透传。`fetch_elements_task` / API `ElementFetchRequest` 增加同名字段（Optional 默认空）。

- [ ] Step 4: 跑测试绿（新 3 个 + 既有 12 个）

- [ ] Step 5: Commit `feat(elements): fetch text/type filters + debug mode (#elem-p1 T1)`

---

### Task 2: bounding box 坐标确认入库

**Files:**
- Verify: `backend/app/services/playwright_locator_core.py` extract_semantic_info 的 coords 来源
- Modify: `backend/app/tasks/element_tasks.py`（若 coords 缺失则补 bounding_box）
- Test: `backend/tests/test_element_tasks.py`

- [ ] Step 1: 读 extract_semantic_info（playwright_locator_core.py:208-251）确认 coords = element.bounding_box() 视口坐标

- [ ] Step 2: 若已是 bounding_box：写一个断言测试锁行为（mock element 的 bounding_box 返回 {x:10,y:20,width:30,height:40} → verified_elements[0] 的 position_x/y/width/height 一致），跳过实现直接 Commit。若不是：修 extract_semantic_info 使 coords 用 bounding_box，再测试。

- [ ] Step 3: Commit `feat(elements): guarantee viewport bounding box in element product (#elem-p1 T2)`

---

### Task 3: 登录态占位接口

**Files:**
- Modify: `backend/app/api/v1/elements.py`（新增 GET /login-state）
- Test: `backend/tests/test_api_elements.py`（新建，参照 test_api_whitescan 的 client fixture 风格）

- [ ] Step 1: 失败测试

```python
def test_login_state_not_configured(client):
    r = client.get("/api/v1/elements/login-state", params={"project_id": str(PID)})
    assert r.status_code == 200
    d = r.json()["data"]
    assert d["status"] == "not_configured"
    assert "cookie_count" in d and "localstorage_count" in d
```

- [ ] Step 2: 实现：查 login_state 表（不存在表则 except 返回 not_configured——P1 无迁移，表查询包 try/except ProgrammingError/OperationalError）

- [ ] Step 3: 测试绿 + Commit `feat(elements): login-state summary endpoint (not_configured placeholder) (#elem-p1 T3)`

---

### Task 4: 前端「从 URL 抓取」弹窗

**Files:**
- Create: `frontend/src/components/element/FetchDialog.vue`
- Modify: `frontend/src/views/ElementLibrary.vue`（抓取卡片改为弹窗触发；结果区复用现有）
- Modify: `frontend/src/api/element.js`（fetchElements 透传新参数 + loginState 方法）

- [ ] Step 1: FetchDialog.vue（el-dialog 720px）：
  - 区块1 基础：项目下拉 + URL 输入（沿用现有逻辑）
  - 区块2 过滤配置：文本过滤输入框（placeholder="如: +,X,删除,新增（逗号分隔，留空不过滤）"）、类型过滤（多选：button/input/select/link/textarea/span）、调试模式 el-switch + 提示文案
  - 区块3 登录态卡片（蓝边框）：调 loginState 接口，未配置显示灰文案；已配置显示 Cookie/localStorage 数
  - 底部：[取消][抓取元素]
  - emit('start', params) 给父组件执行现有 handleFetch 流程（SSE 直播不动）
- [ ] Step 2: ElementLibrary.vue：原 inline 表单替换为 [一次性抓取] 按钮 + FetchDialog；handleFetch 改从 dialog 参数取值（加 text_filter/type_filter/debug_mode 透传）
- [ ] Step 3: `npx vite build` 绿
- [ ] Step 4: Commit `feat(elements): fetch dialog with filters/debug/login-state card (#elem-p1 T4)`

---

### Task 5: 结果分组 + 验证收尾

**Files:**
- Modify: `frontend/src/views/ElementLibrary.vue`（结果区按 element_type 分组表头 + 摘要条更新）

- [ ] Step 1: 结果摘要条显示过滤信息：`共识别 N 个元素（类型: button,input · 文本: +,删除）`；debug 模式加 badge「调试模式」
- [ ] Step 2: 全量后端测试 + vite build
```bash
cd backend && timeout 400 python -m pytest tests/ -q -p no:cacheprovider --ignore=tests/test_batch_import_fix.py --deselect tests/test_storage_get_object.py
cd frontend && npx vite build
```
- [ ] Step 3: Commit `feat(elements): result summary with filter info (#elem-p1 T5)`
- [ ] Step 4: 真实验证（controller 执行）：对 localhost:3000 抓取带 text_filter=刷新 → 只剩刷新按钮；入库后 DB 坐标非空

---

## 注意事项
- **不要动**：SSE 链路（sse.py/element_tasks.py 的直播段）、import 端点、_persist_fetch_result
- 工作区其他会话改动已全部提交（e536afd），开工前 git status 确认干净
- 每个 Task 独立 commit，全绿才进下一个
