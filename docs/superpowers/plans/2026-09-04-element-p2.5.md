# 元素库增强 P2.5（布局调整 + 文本元素抓取）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 用户反馈三项——①截图放大（列表缩小）②全选移出表头、一键入库移到列表底部 ③支持抓取页面**文字/不可点击元素**（当前只扫可交互元素）。

**来源:** 用户 P2 验收反馈（2026-09-04）。

---

### Task 1: 布局调整（纯前端，小改）

**Files:**
- Modify: `frontend/src/views/ElementLibrary.vue`

- [ ] Step 1: 调整
  - 左栏（截图）`:span="12"` → `:span="16"`；右栏（列表）→ `:span="8"`
  - `.screenshot { max-height: 600px }` → `max-height: none`（截图完整显示，宽随栏宽自适应）
  - `.element-list { max-height: 500px }` → `max-height: 400px`（列表收窄）
  - 「全选」checkbox 从卡片 header 移除 → 移到 `.element-list` 顶部（列表内第一行，粘性 top:0 白底）
  - 「一键入库 (N)」按钮从 header 移除 → 移到 `.element-list` 底部（粘性 bottom，宽 100%，保留 primary 样式）
- [ ] Step 2: `npx vite build` 绿
- [ ] Step 3: Commit `feat(elements): result layout — bigger screenshot, select-all/import relocated (#elem-p2.5 T1)`

---

### Task 2: 文本/不可点击元素抓取（后端 + 前端开关）

**Files:**
- Modify: `backend/app/services/playwright_locator_core.py`（scan 支持 include_text 模式）
- Modify: `backend/app/schemas/element_schema.py` + `backend/app/api/v1/elements.py` + `backend/app/tasks/element_tasks.py`（透传）
- Modify: `frontend/src/components/element/FetchDialog.vue`（开关）+ `ElementLibrary.vue`（透传）
- Test: `backend/tests/test_element_tasks.py`

- [ ] Step 1: 失败测试

```python
class TestScanTextElements:
    def test_scan_interactive_with_text(self):
        """include_text=True 时扫描含文本的 span/p/label 等非交互元素."""
        # mock page: 现有交互选择器返回 1 个; 文本选择器 ("span, p, h1, h2, h3, label, td, th" 拆分)
        # 返回 2 个; 验证返回总数 3 且文本元素 text 非空才保留 (空文本 div 占位丢弃)
```

（实现要点：`scan_interactive_elements(page, include_text=False)`；include_text 时在交互选择器后追加文本选择器扫描——每个文本元素**必须有非空 inner_text** 才保留（过滤布局占位），坐标去重逻辑复用（交互元素与文本元素重叠时交互优先——先扫交互、坐标去重集合沿用，文本元素撞坐标即跳过）。element_type 归一化沿用 tagName（span/p/h1...），加 `{"a": "link"}` 映射已有。）

- [ ] Step 2: `text_filter`/`type_filter` 对文本元素同样生效（typeOptions 前端加 span/p/h1/h2/h3/label/td）——在 Task 1 的 `_apply_filters` 无需改动，天然生效；补一个组合测试
- [ ] Step 3: 实现 + 透传（ElementFetchRequest 加 `include_text: bool = False`；FetchDialog 加 el-switch「抓取文字元素」+ 提示文案）
- [ ] Step 4: 测试绿 + vite build 绿
- [ ] Step 5: Commit `feat(elements): capture text/static elements via include_text switch (#elem-p2.5 T2)`

---

### Task 3: 验证收尾

- [ ] Step 1: 全量后端测试 + vite build
- [ ] Step 2: Commit（如有 fixup）
- [ ] Step 3: 用户验收：布局（截图大/全选在列表头内/入库按钮在底部）；开关开启后抓 localhost:3000 能抓到「首页」等文字元素；文本过滤+文字元素组合过滤正常
