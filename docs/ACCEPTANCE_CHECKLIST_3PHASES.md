# 重构三阶段 · 整体验收清单

> 三个阶段已全部合并 master（阶段1 元素资产 / 阶段2 转脚本+测试集 / 阶段3 回归+菜单+登录态）。
> 服务：backend 8000（已生效）+ 前端 build 已更新 + celery worker。打开 http://localhost:3000 逐项验收。
> 标注 ⚠️ 的项是已知限制，验收时留意是否符合预期。

---

## 一、菜单结构（阶段3 改造）

**改造内容**：菜单从 14 项重组为三组结构；测试规则管理/生成历史/执行记录与报告三项从菜单隐藏（直链仍可访问）。

| # | 验收点 | 期望 |
|---|---|---|
| 1.1 | 侧边栏分组 | 仪表盘 → 项目管理 → **用例资产**（用例管理/用例评审与精修/AI智能用例生成/知识库管理）→ **元素资产**（元素抓取/元素管理）→ **自动化**（用例转自动化脚本/UI自动化测试/回归自动化）→ **白盒测试**（单独一级）→ 接口与执行（disabled）→ 系统设置 |
| 1.2 | 隐藏菜单直链 | 直接访问 /ai/rules、/ai/history、/reports 页面仍正常打开 |
| 1.3 | 全部菜单点击 | 无白屏、无 Console 报错 |

## 二、元素资产（阶段1）

**改造内容**：元素库拆为「元素抓取 / 元素管理」两页；新增多定位器置信度排序（用户调序最高优先）、全局共享元素、页面树层级管理、回收站、引用计数保护、快速校验、导入导出。

### 元素抓取（/elements/capture，原元素库页整体迁入）
| # | 验收点 | 期望 |
|---|---|---|
| 2.1 | 访问 /elements | 重定向到 /elements/capture，原一次性抓取+会话式抓取功能完好 |
| 2.2 | 抓取入库 | 元素入库后带 score 评分（抓取时当场验证：唯一命中加分/多命中扣分），按 score 降序存储 |

### 元素管理（/elements/list，新页面）
| # | 验收点 | 期望 |
|---|---|---|
| 2.3 | 页面树 | 选项目 → 左树（📁全部元素/🌐全局元素/页面节点带元素数）；选根=全部，选页=该页+全局元素 |
| 2.4 | 页面树右键 | 重命名/上移/下移/删除（有子页面拒绝；有元素须指定迁移目标或连元素进回收站）；刷新后顺序保持 |
| 2.5 | 元素表格 | 列：元素/类型/首选定位(code样式)/作用域tag/状态/引用数/来源/更新时间/操作 |
| 2.6 | 详情抽屉 | 定位器列表（★=score最高）+ ↑↓ 调序（score 跟随位置，**后端消费端按 score 选定位器**）+「+自定义定位器」+「校验首选定位」（真实页面跑一次，返回命中数） |
| 2.7 | 新建元素 | 页面级须选页面；全局级（导航/菜单类）不挂页面，任何页面下可见 |
| 2.8 | 删除保护 | 有脚本引用的元素删除时弹确认框带引用数；删除进回收站；回收站可恢复 |
| 2.9 | 导入导出 | 导出 JSON 下载；导入跨项目复用（页面级元素按页面名匹配目标项目，未匹配跳过并提示） |

## 三、AI智能用例生成（阶段2 起持续优化，非重构改造，略验）

| # | 验收点 | 期望 |
|---|---|---|
| 3.1 | 生成→转脚本链路 | 生成的用例转脚本后，脚本 step_mapping 带可编辑步骤（阶段2 编辑器消费） |

## 四、用例转自动化脚本（/ai/convert，阶段2 核心改造）

**改造内容**：页面树勾选用例 → 转换弹窗 SSE 直播 → 转换结果可**查看代码 + 步骤化编辑** → 存为测试集。脚本库/快速运行迁往 UI自动化测试页。

| # | 验收点 | 期望 |
|---|---|---|
| 4.1 | 页面树勾选 | 左树按页面/测试点分组（数据来自测试点映射），右侧勾选用例，用例带「可自动化/部分/仅手工」评级 tag |
| 4.2 | 批量转脚本 | 弹窗 SSE 直播逐条转换进度；完成后弹结果列表 |
| 4.3 | **编辑脚本（核心）** | 结果列表「编辑脚本」→ StepEditor 每行=操作类型中文下拉（打开页面/点击/输入/下拉选择/等待/断言文本/断言可见/**数据库断言**）+ 元素定位 + 参数；**从转换产物还原的步骤 action/定位正确**（此前有字段错位 bug 已修） |
| 4.4 | 保存脚本 | 改参数/加步骤 → 保存 → 重新打开确认已保存；保存后生成新 Playwright 代码（版本+1） |
| 4.5 | 数据库断言 | 操作类型选「数据库断言」→ 多行 SQL + 期望值 → 保存（执行侧见 5.5） |
| 4.6 | 存为测试集 | 弹窗内「存为测试集」输入名称 → 成功提示；重名提示后端 detail |

## 五、UI自动化测试（/auto/ui，阶段2 新页面）

**改造内容**：测试集规划与执行 + 报告（失败截图内嵌）+ 脚本库（从转脚本页迁移，含编辑/诊断/快速运行）。

| # | 验收点 | 期望 |
|---|---|---|
| 5.1 | 测试集 Tab | 测试集列表（名称/来源/用例数/通过率/状态）；详情含用例表可移除 |
| 5.2 | 执行测试集 | 执行配置弹窗（**无头 checkbox**/失败即停 fail_fast/超时）→ SSE 直播逐条执行 → 完成自动加载报告 |
| 5.3 | 报告 | 摘要 badge（通过率/总数/耗时）+ 逐用例 ✅❌ + 错误类型中文（定位失败/超时/断言失败/脚本错误）+ **失败截图内嵌**（点击放大） |
| 5.4 | 脚本库 Tab | 从转脚本页迁移：筛选/运行/批量运行/确认入库/**编辑脚本（StepEditor）**/⚡快速运行（粘贴脚本即跑） |
| 5.5 | ⚠️ 编辑器脚本执行 | **假成功已修**：编辑器保存的脚本执行时每步真实执行（此前 0 步假通过）；但 **assert_db 数据库断言的执行**是新增能力，如遇问题优先反馈 |
| 5.6 | ⚠️ AI 诊断 | 失败脚本行「诊断」按钮（迁移后恢复的入口）→ 诊断卡片 |

## 六、回归自动化（/auto/regression，阶段3 改造）

**改造内容**：数据源从「脚本库 included 标记」切到 `for_regression`——白盒生成的用例转脚本后**自动进回归集**；UI自动化页脚本可手动加入回归集（后端就绪）；AI 六规则识别保留为辅助建议。

| # | 验收点 | 期望 |
|---|---|---|
| 6.1 | 白盒自动归集 | 白盒扫描生成的用例 → 评审 → 转脚本 → 自动出现在回归集列表（标识来源=白盒） |
| 6.2 | AI 识别 | 🤖 识别按钮可用，建议列表正常；**⚠️ 确认：AI 建议纳入会覆盖之前人工移出的决定**（当前设计如此，若不满意记下来调） |
| 6.3 | 加入/移出 | 操作生效，列表刷新后状态保持 |
| 6.4 | 执行回归 | 批量执行（fail_fast 策略）+ 回归报告 |
| 6.5 | 前端对齐 | 「已纳入」列显示正确（此前曾因字段切换全显未纳入，已修） |

## 七、用例管理（阶段3 小改造）

| # | 验收点 | 期望 |
|---|---|---|
| 7.1 | 白盒来源标识 | 白盒生成的用例在列表可见来源（source_type） |
| 7.2 | 来源筛选 | 按来源筛选（ai_gen/whitescan/manual）可用 |

## 八、用例评审与精修（阶段3 改造）

**改造内容**：「应用全部建议」之前只改建议状态不改用例（你报过的 bug）——结构类建议自动落地（缺前置自动补）+ **断言增强类走 LLM 真实改写步骤**。

| # | 验收点 | 期望 |
|---|---|---|
| 8.1 | 触发精修 | 含软断言（观察/查看/验证）的用例精修出「断言增强」建议 |
| 8.2 | **应用建议（核心）** | 应用后**步骤实际改写**：软断言词（查看/观察）被 LLM 改为硬断言动作+可断言期望；前置条件缺失的自动补全；用例内容肉眼可见变化 |
| 8.3 | ⚠️ LLM 依赖 | 改写走 GLM，LLM 不可用时降级为只标记（不报错） |

## 九、登录态复用（阶段3，⚠️ 部分能力）

**改造内容**：环境管理新增登录配置 API + 测试登录端点；LoginStateService（storage_state 缓存/TTL/失效）。**⚠️ 执行链路自动注入未激活**（执行配置与环境记录的关联字段未落地，激活需要 RunConfig 扩展——已记待办）。

| # | 验收点 | 期望 |
|---|---|---|
| 9.1 | 登录配置 API | PUT /api/v1/system/envs/{id}/login-config 可写入（**前端表单未做**，用 Swagger 或 curl 验） |
| 9.2 | 测试登录 | POST /api/v1/system/envs/{id}/test-login 真跑登录返回成功/失败 |
| 9.3 | ⚠️ 已知限制 | 脚本执行时尚未自动免登录（链路激活是待办）；账密明文存储（脱敏是待办） |

## 十、已知问题清单（验收时不用重复报）

1. test_api_elements 偶发失败（测试隔离问题，单跑必过）
2. credentials 明文存储/返回（脱敏待办）
3. 执行链路登录态自动注入未激活（待办）
4. AI 识别覆盖人工移出（设计取舍，待你确认）
5. StepEditor「AI 生成 SQL」按钮未实现（二期）
6. 引用数 N+1 串行查询（元素多时略慢）
7. PytestCollectionWarning 消音未做

---

**验收通过标准**：以上非 ⚠️ 项全部符合；⚠️ 项行为与描述一致。发现问题贴给我（页面+操作步骤+现象），我定位修复。全部通过则重构整体收官。
