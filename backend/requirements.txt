# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.8.0
pydantic-settings==2.4.0

# Database
sqlalchemy==2.0.31
psycopg2-binary==2.9.9
asyncpg==0.29.0
alembic==1.13.2

# Redis & Cache
redis==5.0.7
hiredis==2.3.2

# Async Task Queue
celery==5.3.4

# Object Storage
minio==7.2.7
boto3==1.34.144

# HTTP Client
httpx==0.27.0
aiohttp==3.9.5

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
# Symmetric encryption for secrets (system_setting / test_env) - app/core/security.py
pycryptodome==3.23.0

# Validation & Serialization
pydantic==2.8.0
email-validator==2.2.0

# Browser Automation
playwright==1.45.0

# Captcha OCR (识别验证码动作 input_captcha)
ddddocr==1.5.6

# AI & NLP
openai==1.35.0
tiktoken==0.7.0

# Data Processing
pandas==2.2.2
openpyxl==3.1.5
python-docx==1.1.2
PyPDF2==3.0.1
markdown==3.6

# Vector Database
pgvector==0.3.2

# Utilities
python-dotenv==1.0.1
python-dateutil==2.9.0
pytz==2024.1

# Testing
pytest==8.0.0
pytest-asyncio==0.23.0
pytest-cov==4.1.0
httpx==0.27.0

# Mind Map / Import-Export
xmind

# Fuzzy Matching (Self-Heal Level2 DOM fuzzy matching)
rapidfuzz>=3.0.0

# Report generation (W6). weasyprint needs GTK/Pango/cairo native deps and
# does not install cleanly on Windows; gated so `pip install -r` still works
# there. The app degrades gracefully (PDF skipped) when weasyprint is absent.
weasyprint; sys_platform != "win32"
Jinja2
